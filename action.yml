name: 'parse'
description: 'parse issue template into json'
inputs:
  branding:
    icon: "check-square"
    color: blue
  inputs:
    body:
      description: 'The body of the issue generated by issue template'
  outputs:
    json:
      description: 'JSON string'
runs:
  using: 'node16'
  main: 'dist/index.js'
